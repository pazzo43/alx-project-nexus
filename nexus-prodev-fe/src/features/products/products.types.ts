User Action → State Update → API Query Params → Backend → Cached Response


// src/features/products/products.types.ts
export interface Product {
  id: number;
  name: string;
  price: number;
  category: number;
}

